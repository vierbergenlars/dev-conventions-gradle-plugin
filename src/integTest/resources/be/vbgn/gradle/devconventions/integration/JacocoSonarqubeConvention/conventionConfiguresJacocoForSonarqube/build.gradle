plugins {
    id "jacoco"
    id "java"
    id "org.sonarqube" version "3.0"
    id "be.vbgn.dev-conventions"
}

assert jacocoTestReport.reports.xml.enabled == true

gradle.taskGraph.whenReady { graph ->
    assert graph.hasTask(jacocoTestReport)
}

tasks.sonarqube {
    onlyIf({ false })
}
